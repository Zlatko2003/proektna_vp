services:
  backend:
    build: ./backend
    container_name: qa-backend
    restart: always
    network_mode: host
    environment:
      NODE_ENV: development
      PORT: 5000
      MONGO_URI: mongodb://127.0.0.1:27017/proektna_vp_db
      JWT_SECRET: orpX7Uv7PAClGhPo5WR6rbHPF0CCBvrVZ03A1C39lx2lMrTrKgFEIOtghE5JmUbE
      EMAIL_USER: test@test.com
      EMAIL_PASS: test123
    volumes:
      - ./backend:/app
      - /app/node_modules

  frontend:
    build: ./frontend
    container_name: qa-frontend
    restart: always
    network_mode: host
    environment:
      NODE_ENV: development
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend